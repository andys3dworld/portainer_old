---
version: "3.1"
services:
  grav:
    image: lscr.io/linuxserver/grav:latest
    container_name: grav
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
    volumes:
      - /virt/grav/config:/config
    ports:
      - 3001:80
    restart: unless-stopped
